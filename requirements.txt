# RecGen3D — Python dependencies for inference.
#
# PyTorch and torch-cluster are NOT listed here: both need to match your CUDA
# toolkit and must be installed from their own index first. See install.sh /
# the "Installation" section of the README.
#
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install torch-cluster==1.6.3 -f https://data.pyg.org/whl/torch-2.5.1+cu121.html
#
# Reference environment: Python 3.10, CUDA 12.1, PyTorch 2.5.1.

# --- core numerics ---
numpy==1.24.4
scipy==1.14.1
einops==0.8.0

# --- model stack ---
pytorch-lightning==2.5.2
torchmetrics==1.6.0
deepspeed==0.17.1
transformers==4.57.3
diffusers==0.30.0
huggingface-hub==0.36.0
safetensors==0.4.4
accelerate==1.1.1
peft==0.16.0
timm==1.0.16
torchdiffeq==0.2.5

# --- config / io ---
omegaconf==2.3.0
PyYAML==6.0.2
wcmatch==10.1
tqdm==4.66.5

# --- geometry & meshing ---
trimesh==4.4.7
pymeshlab==2022.2.post3
scikit-image==0.24.0

# --- images / io ---
pillow==11.1.0
requests==2.34.2
opencv-python==4.10.0.84
matplotlib==3.10.3
